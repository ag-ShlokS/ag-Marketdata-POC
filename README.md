# ag-Marketdata-POC

A Python-based Proof of Concept for stock market data analysis and visualization.

## Features
- Stock price data fetching using yfinance
- Data analysis and manipulation using pandas
- Interactive visualizations using plotly
- REST API using FastAPI

## Setup
1. Create a virtual environment:
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

2. Install dependencies:
```bash
pip install -r requirements.txt
```

## Project Structure
- `src/` - Source code directory
  - `data/` - Data fetching and processing
  - `api/` - API endpoints
  - `visualization/` - Data visualization components
- `tests/` - Test files
- `config/` - Configuration files

## Usage
[To be added as features are implemented]